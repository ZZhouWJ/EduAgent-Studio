# TASK-017 最终界面打磨、运行脚本、截图清单与结课报告素材整理

## 任务目标

完成系统最终收尾工作：补充前后端运行说明、演示流程、截图清单、课程报告测试素材和全项目最终检查清单。允许进行小幅 UI 打磨，但不得新增业务模块、后端接口或数据库结构。

## 允许实现

1. 前端 README 补充；
2. 前端 `.env.example` 检查；
3. 前端路由清单整理；
4. 系统演示流程说明；
5. 截图清单；
6. 后端 + 前端启动说明整合；
7. 课程报告系统测试章节素材补充；
8. 可选 UI 小幅美化；
9. 全项目最终检查清单。

## 允许修改

- `frontend/README.md`
- `frontend/.env.example`
- `frontend/src` 下与小幅 UI 打磨相关的文件
- `backend/README.md`
- `backend/scripts/route_list.md`
- `backend/scripts/test_report_material.md`
- `cursor_and_codex_chat/handoff/HANDOFF-017-final-polish-run-report.md`
- 如需新增整理文档，可放在 `cursor_and_codex_chat/` 或 `docs/` 下，但不得修改数据库 Schema 冻结文档。

## 禁止修改

- `database/*`
- `docs/01_数据库Schema冻结说明.md`
- 后端 service / repository / router 业务逻辑文件，除非仅修正文档引用或运行说明，且必须在 handoff 中说明。

## 禁止实现

1. 新后端接口；
2. 新数据库表；
3. 新业务模块；
4. 大规模重构；
5. 新的大模型调用能力；
6. 新的审核、成果库、统计业务规则。

## 具体任务

### 一、前端 README 补充

请补充或确认：

1. 前端技术栈；
2. 基于 V3 Admin Vite 二次开发说明；
3. 许可证与 NOTICE 说明；
4. 安装依赖命令；
5. 本地启动命令；
6. 构建命令；
7. `.env.example` 使用说明；
8. API baseURL 配置说明；
9. 与后端 FastAPI 联调说明；
10. 常见问题，例如 Node 版本、接口 401、跨域、后端未启动等。

### 二、前端 `.env.example` 检查

请确认：

1. `VITE_API_BASE_URL` 指向本项目 FastAPI 后端占位地址；
2. 不包含 Apifox Mock 地址；
3. 不包含真实 token；
4. 不包含真实 API Key；
5. 不包含真实数据库密码；
6. 与 request 封装读取的环境变量名一致。

### 三、前端路由清单整理

请整理当前前端已实现路由，至少包含：

1. `/login`；
2. `/dashboard`；
3. `/projects`；
4. `/projects/:projectId`；
5. `/tasks`；
6. `/tasks/:taskId`；
7. `/reviews`；
8. `/reviews/:requestId`；
9. `/artifacts`；
10. `/artifacts/:adoptedId`；
11. `/statistics`；
12. `/models`；
13. `/404`。

说明每个路由对应页面、主要接口、截图建议。

### 四、系统演示流程说明

请整理适合课程答辩的演示流程，例如：

1. 登录系统；
2. 查看首页和整体布局；
3. 创建或查看项目；
4. 查看项目成员和任务；
5. 查看任务详情、分支和输出版本；
6. 执行 AI Mock 生成；
7. 编辑输出、另存新版本、添加批注；
8. 提交审核；
9. 完成审核；
10. 采用成果；
11. 查看成果库；
12. 查看统计看板。

每一步应标明建议截图点和涉及接口。

### 五、截图清单

请整理课程报告截图清单，至少覆盖：

1. 登录页；
2. 后台首页；
3. 项目列表；
4. 项目详情；
5. 任务详情；
6. AI 生成面板；
7. 输出详情；
8. 编辑输出；
9. 批注列表；
10. 审核中心列表；
11. 审核详情和完成审核弹窗；
12. 成果库列表；
13. 成果详情；
14. 统计看板；
15. 后端健康检查；
16. 后端语法检查或启动日志。

### 六、后端 + 前端启动说明整合

请整理完整启动顺序：

1. 初始化 MySQL 数据库；
2. 配置 backend `.env`；
3. 安装后端依赖；
4. 启动 FastAPI；
5. 配置 frontend `.env`；
6. 安装前端依赖；
7. 启动 Vite；
8. 浏览器访问地址；
9. 登录测试账号说明；
10. Windows MySQL 与 WSL / Ubuntu 联调注意事项。

不得虚构已经在不可访问环境中完成真实数据库联调。

### 七、课程报告系统测试章节素材补充

请整理测试素材：

1. 测试目标；
2. 测试环境；
3. 测试范围；
4. 功能测试用例表；
5. 权限测试用例表；
6. 异常测试用例表；
7. 运行验证记录；
8. 已知环境限制；
9. 待补充截图位置。

### 八、可选 UI 小幅美化

允许小幅优化：

1. 页面标题、说明文案；
2. 空状态；
3. loading 状态；
4. 表格列宽；
5. Tag 颜色；
6. 卡片间距；
7. 截图观感。

禁止大规模重构模板、重写布局系统或新增完整业务模块。

### 九、全项目最终检查清单

请创建或补充最终检查清单，至少包含：

1. 数据库脚本 01-07；
2. 后端依赖和启动；
3. 后端 API 路由；
4. 前端依赖和启动；
5. 前端路由；
6. 关键接口联调；
7. 敏感信息检查；
8. Mock API 清理；
9. 开源许可证说明；
10. 课程报告材料。

## 验收要求

1. 不新增后端接口；
2. 不修改数据库结构；
3. 不新增业务模块；
4. 文档和截图清单能直接支持课程报告整理；
5. README 中不包含真实密码、真实 API Key、真实 JWT Secret 或完整 `sk-` 密钥；
6. 不声称未实际完成的 MySQL 导入或接口联调已经全部通过；
7. 如果当前环境无法执行 Node / MySQL / 后端服务，应明确写出环境限制。

## Handoff 要求

完成后创建：

`cursor_and_codex_chat/handoff/HANDOFF-017-final-polish-run-report.md`

handoff 必须包含：

1. 修改文件清单；
2. 文档补充内容摘要；
3. 是否执行前端构建；
4. 是否执行后端检查；
5. 未能执行的命令及原因；
6. 是否发现敏感信息；
7. 是否存在仍需人工补充的截图或测试记录。
