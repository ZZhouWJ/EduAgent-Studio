# 系统测试与结果分析

> 本文档为智研协作 AI 项目质量审计系统的系统测试与结果分析，供课程报告"系统测试与结果分析"章节使用。
> 测试执行人与日期请在实际执行时填写。
>
> **重要声明**：本测试报告如实记录当前环境下的测试结果，不虚构或伪造任何测试通过截图。凡是需要 MySQL 数据库连接的测试项，报告中均标注"待补充截图"或"待本地 Windows MySQL 环境验证"。

---

## 1. 测试环境

### 1.1 硬件与操作系统

| 项目 | 环境说明 |
|------|---------|
| **操作系统** | Windows 10/11（本地完整环境）或 Ubuntu 22.04（WSL2）|
| **Python 版本** | Python 3.10+ |
| **Node.js 版本** | Node.js 18+（前端构建与开发服务器）|
| **数据库** | MySQL 8.0 |

### 1.2 后端测试环境

| 项目 | 环境说明 |
|------|---------|
| **Web 框架** | FastAPI >= 0.110.0 |
| **ASGI 服务器** | uvicorn >= 0.27.0 |
| **数据库驱动** | PyMySQL >= 1.1.0（无 ORM，使用原生参数化 SQL）|
| **依赖管理** | 见 `backend/requirements.txt` |
| **启动命令** | `uvicorn app.main:app --reload --host 0.0.0.0 --port 8000` 或 `python run.py` |

### 1.3 前端测试环境

| 项目 | 环境说明 |
|------|---------|
| **框架** | Vue 3 (Composition API) + TypeScript |
| **构建工具** | Vite 7 |
| **依赖管理** | npm 或 pnpm |
| **安装命令** | `npm install` |
| **开发服务器** | `npm run dev`（端口 5173）|
| **生产构建** | `npm run build` |

### 1.4 数据库环境

| 项目 | 环境说明 |
|------|---------|
| **数据库** | MySQL 8.0+ |
| **数据库名** | `ai_collab_audit_system` |
| **初始化脚本** | `database/01` 到 `07`，按顺序执行 |

### 1.5 环境限制说明

> **⚠️ 当前远程 Ubuntu/WSL2 环境存在以下限制，请在本地 Windows 完整环境中补做相关测试与截图：**

| 限制项 | 影响范围 | 缓解措施 |
|--------|----------|----------|
| 远程环境无 Node | 无法执行 `npm install` / `npm run dev` / `npm run build` | 在本地 Windows Node 环境下补做前端构建与运行验证 |
| WSL2 无法访问 Windows MySQL | 涉及数据库读写的接口无法在远程环境验证 | 在 Windows MySQL 可连接环境中补做，或配置 WSL2 网络访问 |
| MySQL 未在远程环境初始化 | `database/` 脚本未执行，数据库为空 | 在 Windows MySQL 环境下执行 01-07 初始化脚本 |

---

## 2. 测试目标

1. **语法正确性验证**：验证后端 Python 代码语法正确，所有模块可正常导入，无 ImportError 或 SyntaxError。
2. **应用启动验证**：验证 FastAPI 应用可正常启动，所有 API 路由正确注册，`/docs` 可正常访问。
3. **数据库连接验证**：验证数据库连接配置正确，`PyMySQL` 能成功建立到 MySQL 的连接。
4. **业务接口验证**：验证核心业务接口返回符合项目统一响应格式（`code:0, message:success, data`）的 JSON 响应。
5. **前端构建验证**：验证前端项目可正常安装依赖并完成生产构建（需 Node 环境）。
6. **报告素材采集**：为课程报告提供可截图、可复现的测试记录素材，所有截图均注明操作步骤与预期结果。

---

## 3. 测试范围

| 测试类型 | 测试内容 | 覆盖状态 |
|---------|---------|---------|
| **Backend 模块语法检查** | 对所有 Python 源文件执行 `py_compile` 编译检查，确保无语法错误 | 当前可验证（不依赖 MySQL）|
| **FastAPI 应用启动** | 验证 uvicorn 可成功加载应用、注册路由、暴露 OpenAPI 文档 | 当前可验证 |
| **数据库连接** | 验证 `/api/health` 和 `/api/health/db` 端点的响应 | 当前部分可验证（`/api/health` 不依赖 DB）|
| **核心业务接口** | 认证模块、项目模块、任务模块、审核模块、统计模块的主要接口 | 待 MySQL 环境补做 |
| **前端构建** | 验证 `npm install` 和 `npm run build` 成功 | 待 Node 环境补做 |
| **前后端联调** | 登录、项目 CRUD、任务 CRUD、AI 生成、审核提交等端到端流程 | 待 MySQL 环境补做 |

> 测试暂不覆盖前端页面的 E2E 测试与 CI/CD 自动化测试流水线。

---

## 4. 功能测试用例

### 4.1 认证与用户模块

| 用例编号 | 测试点 | 测试步骤 | 预期结果 | 状态 |
|---------|--------|---------|---------|------|
| TC-001 | 用户登录 | `POST /api/auth/login`，body: `{"username":"admin","password":"<PLACEHOLDER_PASSWORD>"}` | 返回 `{"code":0,"message":"success","data":{"token":"...","user":{...}}}` | 待 MySQL |
| TC-002 | 获取当前用户 | `GET /api/auth/me`，Header: `Authorization: Bearer <token>` | 返回 `{"code":0,"message":"success","data":{"user_id":...,"username":"..."}}` | 待 MySQL |
| TC-003 | 用户登出 | `POST /api/auth/logout`，Header: `Authorization: Bearer <token>` | 返回 `{"code":0,"message":"success","data":null}` | 待 MySQL |
| TC-004 | 登录失败 | `POST /api/auth/login`，错误的密码 | 返回 `{"code":4002,"message":"用户名或密码错误","data":null}` | 待 MySQL |

### 4.2 项目空间模块

| 用例编号 | 测试点 | 测试步骤 | 预期结果 | 状态 |
|---------|--------|---------|---------|------|
| TC-005 | 项目列表 | `GET /api/projects`，Header: `Authorization: Bearer <token>` | 返回 `{"code":0,"message":"success","data":{"items":[...],"total":...}}` | 待 MySQL |
| TC-006 | 创建项目 | `POST /api/projects`，body: `{"project_name":"测试项目","project_type":"course_project","description":"测试描述"}` | 返回 `{"code":0,"message":"success","data":{"project_id":...}}` | 待 MySQL |
| TC-007 | 项目详情 | `GET /api/projects/1`，Header: `Authorization: Bearer <token>` | 返回 `{"code":0,"message":"success","data":{"project_id":...,"project_name":"..."}}` | 待 MySQL |
| TC-008 | 更新项目 | `PUT /api/projects/1`，body: `{"project_name":"新名称"}` | 返回 `{"code":0,"message":"success","data":null}` | 待 MySQL |
| TC-009 | 添加项目成员 | `POST /api/projects/1/members`，body: `{"user_id":2,"role":"member"}` | 返回 `{"code":0,"message":"success","data":{"member_id":...}}` | 待 MySQL |
| TC-010 | 删除项目（软删除）| `DELETE /api/projects/1` | 返回 `{"code":0,"message":"success","data":null}` | 待 MySQL |

### 4.3 任务与版本模块

| 用例编号 | 测试点 | 测试步骤 | 预期结果 | 状态 |
|---------|--------|---------|---------|------|
| TC-011 | 创建任务 | `POST /api/projects/1/tasks`，body: `{"task_type_id":1,"title":"生成需求分析初稿","description":"...","assignee_id":2,"priority":"normal","due_date":"2026-06-10 23:59:59"}` | 返回 `{"code":0,"message":"success","data":{"task_id":...}}` | 待 MySQL |
| TC-012 | 项目任务列表 | `GET /api/projects/1/tasks`，Header: `Authorization: Bearer <token>` | 返回 `{"code":0,"message":"success","data":{"items":[...],"total":...}}` | 待 MySQL |
| TC-013 | 任务详情 | `GET /api/tasks/1`，Header: `Authorization: Bearer <token>` | 返回 `{"code":0,"message":"success","data":{"task_id":...,"title":"..."}}` | 待 MySQL |
| TC-014 | 更新任务 | `PUT /api/tasks/1`，body: `{"title":"更新后的标题"}` | 返回 `{"code":0,"message":"success","data":null}` | 待 MySQL |
| TC-015 | 创建任务分支 | `POST /api/tasks/1/branches`，body: `{"branch_name":"v2","parent_branch_id":1}` | 返回 `{"code":0,"message":"success","data":{"branch_id":...}}` | 待 MySQL |
| TC-016 | 输出版本列表 | `GET /api/tasks/1/outputs`，Header: `Authorization: Bearer <token>` | 返回 `{"code":0,"message":"success","data":{"items":[...],"total":...}}` | 待 MySQL |
| TC-017 | 输出版本详情 | `GET /api/outputs/1`，Header: `Authorization: Bearer <token>` | 返回 `{"code":0,"message":"success","data":{"output_id":...,"content":"..."}}` | 待 MySQL |

### 4.4 AI 生成模块

| 用例编号 | 测试点 | 测试步骤 | 预期结果 | 状态 |
|---------|--------|---------|---------|------|
| TC-018 | AI 生成输出（Mock）| `POST /api/tasks/1/generate`，body: `{"model_ids":[1],"input_text":"生成需求分析","branch_id":1}` | 返回 `{"code":0,"message":"success","data":[{output_id:...,model_id:...}]}` | 当前可验证 |
| TC-019 | 调用日志列表 | `GET /api/invocations`，Header: `Authorization: Bearer <token>` | 返回 `{"code":0,"message":"success","data":{"items":[...],"total":...}}` | 待 MySQL |
| TC-020 | 模型列表 | `GET /api/ai-models`，Header: `Authorization: Bearer <token>` | 返回 `{"code":0,"message":"success","data":{"items":[...],"total":...}}` | 当前可验证（不依赖 DB）|

### 4.5 人工编辑与批注模块

| 用例编号 | 测试点 | 测试步骤 | 预期结果 | 状态 |
|---------|--------|---------|---------|------|
| TC-021 | 更新输出 | `PUT /api/outputs/1`，body: `{"content":"更新后的内容","version":1}` | 返回 `{"code":0,"message":"success","data":null}` | 待 MySQL |
| TC-022 | 另存为新版本 | `POST /api/outputs/1/save-as-new-version`，body: `{"content":"新版本内容"}` | 返回 `{"code":0,"message":"success","data":{"output_id":...}}` | 待 MySQL |
| TC-023 | 添加批注 | `POST /api/outputs/1/comments`，body: `{"content":"第三条逻辑不够清晰"}` | 返回 `{"code":0,"message":"success","data":{"comment_id":...}}` | 待 MySQL |
| TC-024 | 批注列表 | `GET /api/outputs/1/comments`，Header: `Authorization: Bearer <token>` | 返回 `{"code":0,"message":"success","data":{"items":[...],"total":...}}` | 待 MySQL |

### 4.6 审核中心模块

| 用例编号 | 测试点 | 测试步骤 | 预期结果 | 状态 |
|---------|--------|---------|---------|------|
| TC-025 | 提交审核 | `POST /api/outputs/1/submit-review`，body: `{"submit_note":"请审核需求分析初稿"}` | 返回 `{"code":0,"message":"success","data":{"request_id":...}}` | 待 MySQL |
| TC-026 | 待审核列表 | `GET /api/reviews/pending`，Header: `Authorization: Bearer <token>` | 返回 `{"code":0,"message":"success","data":{"items":[...],"total":...}}` | 待 MySQL |
| TC-027 | 审核详情 | `GET /api/reviews/1`，Header: `Authorization: Bearer <token>` | 返回 `{"code":0,"message":"success","data":{"request_id":...,"output_content":"..."}}` | 待 MySQL |
| TC-028 | 完成审核 | `POST /api/reviews/1/complete`，body: `{"review_status":"approved","accuracy_score":9,"completeness_score":8,"logic_score":9,"format_score":8,"usability_score":9,"risk_score":1,"review_comment":"可用","issue_tag_ids":[]}` | 返回 `{"code":0,"message":"success","data":{"request_id":...}}` | 待 MySQL |
| TC-029 | 问题标签列表 | `GET /api/issue-tags`，Header: `Authorization: Bearer <token>` | 返回 `{"code":0,"message":"success","data":[...]}` | 待 MySQL |

### 4.7 成果库模块

| 用例编号 | 测试点 | 测试步骤 | 预期结果 | 状态 |
|---------|--------|---------|---------|------|
| TC-030 | 采用输出为成果 | `POST /api/outputs/1/adopt`，body: `{"artifact_title":"最终报告","artifact_type":"document","release_version":"v1.0","adopt_note":"采用"}` | 返回 `{"code":0,"message":"success","data":{"adopted_id":...}}` | 待 MySQL |
| TC-031 | 项目成果列表 | `GET /api/projects/1/artifacts`，Header: `Authorization: Bearer <token>` | 返回 `{"code":0,"message":"success","data":{"items":[...],"total":...}}` | 待 MySQL |
| TC-032 | 成果详情 | `GET /api/artifacts/1`，Header: `Authorization: Bearer <token>` | 返回 `{"code":0,"message":"success","data":{"adopted_id":...,"artifact_title":"...","content":"..."}}` | 待 MySQL |

### 4.8 统计看板模块

| 用例编号 | 测试点 | 测试步骤 | 预期结果 | 状态 |
|---------|--------|---------|---------|------|
| TC-033 | 统计概览 | `GET /api/statistics/overview`，Header: `Authorization: Bearer <token>` | 返回 `{"code":0,"message":"success","data":{"project_count":...,"active_project_count":...,"task_count":...,"pending_review_count":...,"invocation_count":...,"success_invocation_count":...,"artifact_count":...,"total_tokens":...,"total_cost":...}}` | 待 MySQL |
| TC-034 | 项目维度统计 | `GET /api/statistics/projects`，Header: `Authorization: Bearer <token>` | 返回 `{"code":0,"message":"success","data":[...]}` | 待 MySQL |
| TC-035 | 模型调用统计 | `GET /api/statistics/model-calls`，Header: `Authorization: Bearer <token>` | 返回 `{"code":0,"message":"success","data":[...]}` | 待 MySQL |
| TC-036 | 成本统计 | `GET /api/statistics/costs`，Header: `Authorization: Bearer <token>` | 返回 `{"code":0,"message":"success","data":{"total_cost":...,"input_cost":...,"output_cost":...,"total_tokens":...,"cost_by_model":[...],"cost_by_project":[...]}}` | 待 MySQL |
| TC-037 | 成员贡献统计 | `GET /api/statistics/member-contributions`，Header: `Authorization: Bearer <token>` | 返回 `{"code":0,"message":"success","data":[{"user_id":...,"real_name":"...","project_count":...,"task_created_count":...,"task_assigned_count":...,"output_created_count":...,"review_count":...,"artifact_adopted_count":...,"invocation_count":...}]}` | 待 MySQL |

---

## 5. 数据库测试用例

### 5.1 建库建表测试

| 用例编号 | 测试点 | 测试步骤 | 预期结果 | 状态 |
|---------|--------|---------|---------|------|
| DB-001 | 创建数据库 | `mysql -u root -p < 01_create_database.sql` | 数据库 `ai_collab_audit_system` 创建成功 | 待本地 MySQL |
| DB-002 | 创建数据表 | `mysql -u root -p ai_collab_audit_system < 02_create_tables.sql` | 20+ 张表创建成功，无报错 | 待本地 MySQL |
| DB-003 | 创建索引 | `mysql -u root -p ai_collab_audit_system < 03_create_indexes.sql` | 索引创建成功，无报错 | 待本地 MySQL |
| DB-004 | 导入初始数据 | `mysql -u root -p ai_collab_audit_system < 04_insert_initial_data.sql` | 角色、用户、任务类型、问题标签等初始数据导入成功 | 待本地 MySQL |
| DB-005 | 创建视图 | `mysql -u root -p ai_collab_audit_system < 05_create_views.sql` | 视图创建成功 | 待本地 MySQL |
| DB-006 | 创建存储过程 | `mysql -u root -p ai_collab_audit_system < 06_create_stored_procedures.sql` | 存储过程创建成功 | 待本地 MySQL |
| DB-007 | 执行测试查询 | `mysql -u root -p ai_collab_audit_system < 07_test_queries.sql` | 测试查询返回预期结果 | 待本地 MySQL |

### 5.2 数据完整性测试

| 用例编号 | 测试点 | 测试步骤 | 预期结果 | 状态 |
|---------|--------|---------|---------|------|
| DB-008 | 外键约束 | 尝试删除被其他表引用的记录 | 外键约束阻止删除或级联删除生效 | 待本地 MySQL |
| DB-009 | 软删除一致性 | 查询已删除记录的关联数据 | 软删除记录不影响正常业务查询结果 | 待本地 MySQL |
| DB-010 | 密码哈希验证 | 验证 `users` 表中密码字段为 BCrypt 哈希（不以明文存储）| 密码字段不以明文存储 | 待本地 MySQL |
| DB-011 | API Key 加密 | 验证 `api_configs` 表中 API Key 为加密存储 | API Key 字段不以明文存储 | 待本地 MySQL |

### 5.3 视图与存储过程测试

| 用例编号 | 测试点 | 测试步骤 | 预期结果 | 状态 |
|---------|--------|---------|---------|------|
| DB-012 | 视图可用性 | `SELECT * FROM v_user_projects LIMIT 1;` | 返回有效的用户项目关联视图数据 | 待本地 MySQL |
| DB-013 | 存储过程可用性 | `CALL sp_get_task_output_stats(1, @total_outputs, @approved_outputs);` | 存储过程正常执行并返回值 | 待本地 MySQL |

---

## 6. 权限测试用例

| 用例编号 | 测试点 | 测试步骤 | 预期结果 | 状态 |
|---------|--------|---------|---------|------|
| AU-001 | 公开接口无需认证 | `GET /api/health` 不带 Token | 返回 `{"code":0,"message":"success","data":{"status":"ok"}}` | 当前可验证 |
| AU-002 | 受保护接口需认证 | `GET /api/projects` 不带 Token | 返回 `{"code":4001,"message":"未登录","data":null}` | 待 MySQL |
| AU-003 | Token 过期 | 使用过期 Token 访问受保护接口 | 返回 `{"code":4003,"message":"Token 已过期","data":null}` | 待 MySQL |
| AU-004 | 管理员可查看用户列表 | `admin` 用户 `GET /api/users` | 返回用户列表 | 待 MySQL |
| AU-005 | 普通用户无法查看用户列表 | `member01` 用户 `GET /api/users` | 返回 `{"code":4001,"message":"权限不足","data":null}` | 待 MySQL |

---

## 7. 事务测试用例

| 用例编号 | 测试点 | 测试步骤 | 预期结果 | 状态 |
|---------|--------|---------|---------|------|
| TX-001 | 乐观锁并发更新 | 两个请求同时 `PUT /api/outputs/1`，其中一个带旧 version 号 | 其中一个返回 `{"code":5003,"message":"版本冲突","data":null}` | 待 MySQL |
| TX-002 | 软删除一致性 | 删除项目后，该项目的任务和输出仍可通过 ID 查询（is_deleted=1） | 软删除仅标记，不级联物理删除业务关联数据 | 待 MySQL |
| TX-003 | 登录日志写入 | 执行登录操作 | `login_logs` 表中新增一条记录 | 待 MySQL |
| TX-004 | 操作日志记录 | 执行创建项目操作 | `operation_logs` 表中新增一条记录 | 待 MySQL |

---

## 8. 前后端联调测试用例

| 用例编号 | 测试点 | 前端操作 | 后端接口 | 预期结果 | 状态 |
|---------|--------|---------|---------|---------|------|
| FE-001 | 登录流程 | 输入用户名和密码，点击登录 | `POST /api/auth/login` | 登录成功，跳转首页，Token 存入 Pinia | 待 MySQL |
| FE-002 | 首页数据加载 | 登录后跳转首页 | `GET /api/statistics/overview` | 首页显示统计卡片数据 | 待 MySQL |
| FE-003 | 项目列表加载 | 进入项目空间 | `GET /api/projects` | 显示项目列表 | 待 MySQL |
| FE-004 | 创建项目 | 点击创建项目，填写表单并提交 | `POST /api/projects` | 新项目出现在列表中 | 待 MySQL |
| FE-005 | 创建任务 | 在项目详情页点击创建任务 | `POST /api/projects/1/tasks` | 任务出现在项目任务列表中 | 待 MySQL |
| FE-006 | AI 生成（Mock）| 在任务详情页点击生成 | `POST /api/tasks/1/generate` | 显示 Mock 生成结果 | 当前可验证 |
| FE-007 | 提交审核 | 在输出详情页点击提交审核 | `POST /api/outputs/1/submit-review` | 审核请求创建成功，审核中心列表中出现该记录 | 待 MySQL |
| FE-008 | 完成审核 | 在审核详情页提交评分 | `POST /api/reviews/1/complete` | 审核状态更新为"已通过" | 待 MySQL |
| FE-009 | 采用成果 | 在输出详情页点击采用成果 | `POST /api/outputs/1/adopt` | 成果出现在成果库中 | 待 MySQL |
| FE-010 | 统计看板 | 进入统计看板页面 | 多个统计接口 | 页面显示所有统计图表 | 待 MySQL |

---

## 9. 测试截图清单

### 9.1 环境准备截图

| 截图编号 | 截图名称 | 截图内容 | 报告章节 | 状态 |
|---------|---------|---------|---------|------|
| SS-01 | Python 版本 | `python --version` 输出，Python 3.10+ | 测试环境 | 当前可截 |
| SS-02 | Node 版本 | `node -v` 输出，Node 18+ | 测试环境 | 待本地 Node |
| SS-03 | npm install 输出 | 前端依赖安装成功 | 测试环境 | 待本地 Node |
| SS-04 | npm run build 输出 | 前端构建成功，无错误 | 测试环境 | 待本地 Node |

### 9.2 后端测试截图

| 截图编号 | 截图名称 | 截图内容 | 报告章节 | 状态 |
|---------|---------|---------|---------|------|
| SS-05 | 后端语法检查 | `python scripts/check_backend.py` 输出，49/49 通过 | 代码质量 | 当前可截 |
| SS-06 | 后端启动 | `python run.py` 输出，`Application startup complete` | 系统实现 | 当前可截 |
| SS-07 | Swagger UI | 浏览器打开 http://localhost:8000/docs | 系统实现 | 当前可截 |
| SS-08 | 健康检查 | `curl http://localhost:8000/api/health` 输出 | 系统测试 | 当前可截 |
| SS-09 | 数据库健康检查 | `curl http://localhost:8000/api/health/db` 输出，`database: connected` | 系统测试 | 待本地 MySQL |
| SS-10 | 用户登录 | Swagger UI 或 curl 调用 `POST /api/auth/login` | 认证测试 | 待本地 MySQL |
| SS-11 | 创建项目 | `POST /api/projects` 调用，返回 `project_id` | 项目管理测试 | 待本地 MySQL |
| SS-12 | 创建任务 | `POST /api/projects/1/tasks` 调用，返回 `task_id` | 任务管理测试 | 待本地 MySQL |
| SS-13 | AI 生成 | `POST /api/tasks/1/generate` 调用，Mock 返回 | AI 生成测试 | 当前可截 |
| SS-14 | 提交审核 | `POST /api/outputs/1/submit-review` 调用 | 审核测试 | 待本地 MySQL |
| SS-15 | 完成审核 | `POST /api/reviews/1/complete` 调用 | 审核测试 | 待本地 MySQL |
| SS-16 | 采用成果 | `POST /api/outputs/1/adopt` 调用 | 成果库测试 | 待本地 MySQL |
| SS-17 | 统计概览 | `GET /api/statistics/overview` 返回 JSON | 统计测试 | 待本地 MySQL |
| SS-18 | 成员贡献 | `GET /api/statistics/member-contributions` 返回 JSON | 统计测试 | 待本地 MySQL |

### 9.3 数据库测试截图

| 截图编号 | 截图名称 | 截图内容 | 报告章节 | 状态 |
|---------|---------|---------|---------|------|
| SS-19 | 数据库表列表 | MySQL 客户端显示 `ai_collab_audit_system` 下所有表（20+ 张）| 数据库设计 | 待本地 MySQL |
| SS-20 | 核心表结构 | Navicat 或 `DESCRIBE` 输出关键表结构 | 数据库设计 | 待本地 MySQL |
| SS-21 | 初始数据 | `SELECT * FROM users LIMIT 5;` 输出 | 数据库设计 | 待本地 MySQL |
| SS-22 | 视图查询 | `SELECT * FROM v_user_projects LIMIT 1;` 输出 | 数据库设计 | 待本地 MySQL |
| SS-23 | 存储过程调用 | `CALL sp_get_task_output_stats(1, @a, @b);` 输出 | 数据库设计 | 待本地 MySQL |

### 9.4 前端测试截图

| 截图编号 | 截图名称 | 截图内容 | 报告章节 | 状态 |
|---------|---------|---------|---------|------|
| SS-24 | 前端登录页 | 浏览器截图 http://localhost:5173 | 系统实现 | 待本地 Node |
| SS-25 | 首页 | 登录后跳转 http://localhost:5173/#/dashboard | 系统实现 | 待本地 Node |
| SS-26 | 项目列表 | http://localhost:5173/#/projects | 系统实现 | 待本地 Node |
| SS-27 | 任务详情 | http://localhost:5173/#/tasks/1，含 AI 生成面板 | 系统实现 | 待本地 Node |
| SS-28 | 审核中心 | http://localhost:5173/#/reviews | 系统实现 | 待本地 Node |
| SS-29 | 成果库 | http://localhost:5173/#/artifacts | 系统实现 | 待本地 Node |
| SS-30 | 统计看板 | http://localhost:5173/#/statistics | 系统实现 | 待本地 Node |

---

## 10. 已知环境限制

| 限制项 | 说明 | 补充方案 |
|--------|------|---------|
| 远程环境无 Node | `node -v` 返回 `command not found`，无法执行 `npm install` / `npm run build` | 在本地 Windows Node 18+ 环境下补做前端构建验证 |
| WSL2 无法访问 Windows MySQL | Ubuntu/WSL2 环境下无法直接连接 Windows 宿主机 MySQL | 配置 `bind-address=0.0.0.0` 或通过 WSL 虚拟网卡 IP 连接，或直接在 Windows 环境下测试 |
| MySQL 数据库未初始化 | `database/01` 到 `07` 脚本尚未在远程环境执行 | 在 Windows MySQL 环境下按顺序执行 01-07 脚本 |
| 接口联调截图待补做 | 涉及数据库读写的接口测试（TC-001 到 TC-037 中标注"待 MySQL"的项目）| 在本地 MySQL 可连接环境中补做，报告中标注"待补充截图" |

---

## 11. 未完成项说明

以下测试项因环境限制当前无法完成，请在本地完整环境中补做：

### 11.1 必须补做（影响报告评分）

| 未完成项 | 原因 | 补做环境 |
|---------|------|---------|
| 数据库建表成功截图 | 远程环境无 MySQL | Windows MySQL 8.0+ |
| 数据库表结构截图（20+ 张表）| 远程环境无 MySQL | Windows MySQL 8.0+ |
| 登录接口端到端测试截图 | 需要数据库中的用户数据 | Windows MySQL + 后端 |
| 项目 CRUD 接口截图 | 需要数据库读写 | Windows MySQL + 后端 |
| 任务 CRUD 接口截图 | 需要数据库读写 | Windows MySQL + 后端 |
| 审核流程端到端截图 | 需要数据库读写 | Windows MySQL + 后端 |
| 成果库端到端截图 | 需要数据库读写 | Windows MySQL + 后端 |
| 统计看板数据截图 | 需要数据库中有数据 | Windows MySQL + 后端 |
| 前端完整页面截图（SS-24 到 SS-30）| 远程环境无 Node | Windows Node 18+ |

### 11.2 诚实声明

> **本测试报告如实记录当前环境下的测试结果，不虚构或伪造任何测试通过截图。** 任何在当前环境中无法实际验证的测试项，报告中均已标注"待补充截图"或"待本地 Windows MySQL 环境验证"。
>
> Stage-17 FIX 修正了所有旧接口路径和旧字段名，确保测试用例与后端真实实现一致。

---

## 12. 接口响应格式规范

### 12.1 统一成功响应

```json
{
  "code": 0,
  "message": "success",
  "data": { ... }
}
```

> **注意**：不得使用 `code: 200` 作为系统统一返回格式，所有成功响应必须使用 `code: 0`。

### 12.2 统一错误响应

```json
{
  "code": 4001,
  "message": "权限不足",
  "data": null
}
```

```json
{
  "code": 4002,
  "message": "用户名或密码错误",
  "data": null
}
```

```json
{
  "code": 5000,
  "message": "内部服务器错误",
  "data": null
}
```

```json
{
  "code": 5003,
  "message": "版本冲突，请重新获取数据后提交",
  "data": null
}
```

---

*本文档版本：v1.1 | 生成日期：2026-06-03 | Stage-MATURE-PRODUCT-FIX | 适用于 AI 协作审核系统课程报告系统测试章节*
