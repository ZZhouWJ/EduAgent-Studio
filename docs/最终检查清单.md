# 最终检查清单

> 本文档为智研协作 AI 项目质量审计系统的最终检查清单，用于课程报告提交前的全面自查。
> 版本：v1.0 | 日期：2026-06-01

---

## 一、数据库脚本检查

| 检查项 | 状态 | 备注 |
|--------|------|------|
| `database/01_create_database.sql` 存在且可执行 | [ ] | 创建 `ai_collab_audit_system` 数据库 |
| `database/02_create_tables.sql` 存在且可执行 | [ ] | 创建所有业务表（约 20+ 张）|
| `database/03_create_indexes.sql` 存在且可执行 | [ ] | 创建索引 |
| `database/04_insert_initial_data.sql` 存在且可执行 | [ ] | 导入测试账号、角色、任务类型等 |
| `database/05_create_views.sql` 存在且可执行 | [ ] | 创建视图 |
| `database/06_create_stored_procedures.sql` 存在且可执行 | [ ] | 创建存储过程 |
| `database/07_test_queries.sql` 存在且可执行 | [ ] | 测试查询 |
| 所有脚本按顺序执行无报错 | [ ] | 需在 MySQL 环境中验证 |
| 外键约束关系正确 | [ ] | projects ↔ tasks ↔ outputs ↔ reviews ↔ artifacts |
| 软删除字段（is_deleted）一致性 | [ ] | 所有业务表均包含且逻辑一致 |

---

## 二、后端检查

| 检查项 | 状态 | 备注 |
|--------|------|------|
| `backend/requirements.txt` 存在 | [ ] | Python 依赖清单 |
| `backend/.env.example` 存在且不含真实密钥 | [ ] | 仅占位符，无 `sk-`、无真实密码 |
| `backend/app/main.py` 可正常导入 | [ ] | FastAPI 应用可加载 |
| `backend/scripts/check_backend.py` 执行通过 | [ ] | 49/49 Python 文件语法正确 |
| `backend/run.py` 可启动服务 | [ ] | `python run.py` 无报错 |
| `/api/health` 返回 `{"code":0}` | [ ] | 服务健康检查 |
| `/api/health/db` 数据库连接正常（需 MySQL）| [ ] | 或返回正确的 disconnected 状态码 |
| `/docs` Swagger UI 可访问 | [ ] | 所有 API 路由已注册 |
| 未使用 `/api/auth/register` 接口 | [ ] | 系统中无用户注册接口 |
| 未使用 `task_name` / `task_type` 字段 | [ ] | 创建任务字段为 `title` 和 `task_type_id` |
| 统一返回格式为 `code:0` | [ ] | 所有成功响应使用 `code:0` |
| JWT 密钥不硬编码 | [ ] | 通过 `os.getenv("JWT_SECRET_KEY")` 读取 |
| API Key 加密存储 | [ ] | AES-GCM 加密，不明文保存 |
| 密码哈希存储 | [ ] | BCrypt，不明文保存 |

---

## 三、前端检查

| 检查项 | 状态 | 备注 |
|--------|------|------|
| `frontend/package.json` 存在 | [ ] | npm 依赖配置 |
| `frontend/.env.example` 存在且不含真实密钥 | [ ] | 仅 `http://127.0.0.1:8000` 占位地址 |
| `frontend/.env` 中无 Apifox Mock 地址 | [ ] | 已清理所有 mock 代理 |
| `frontend/vite.config.ts` 无 mock 代理配置 | [ ] | 已移除 mock 反向代理 |
| `npm install` 可正常执行（需 Node）| [ ] | 依赖安装无报错 |
| `npm run dev` 可正常启动（需 Node）| [ ] | Vite 开发服务器正常 |
| `npm run build` 可正常构建（需 Node）| [ ] | 生产版本无编译错误 |
| 登录页对接 `/api/auth/login` | [ ] | 非 Mock 接口 |
| 首页对接 `/api/statistics/overview` | [ ] | 非 Mock 接口 |
| 所有业务页面已实现（非仅占位）| [ ] | 审核中心、成果库、统计看板等 |
| `frontend/NOTICE.md` 存在 | [ ] | 开源归属说明 |
| `frontend/LICENSE` 存在 | [ ] | MIT 许可证 |

---

## 四、文档检查

| 检查项 | 状态 | 备注 |
|--------|------|------|
| 根 `README.md` 存在且完整 | [ ] | 包含所有 10 个必需章节 |
| `backend/README.md` 存在且完整 | [ ] | 后端启动说明、API 格式说明 |
| `frontend/README.md` 存在且完整 | [ ] | 前端说明、V3 Admin Vite 归属 |
| `frontend/scripts/route_list.md` 存在 | [ ] | 前端路由清单 |
| `docs/系统演示流程.md` 存在 | [ ] | 答辩演示流程 |
| `docs/截图清单.md` 存在 | [ ] | 课程报告截图清单 |
| `docs/最终检查清单.md` 存在 | [ ] | 本文档 |
| `backend/scripts/test_report_material.md` 存在且最新 | [ ] | 测试报告素材 |
| `backend/scripts/route_list.md` 存在且最新 | [ ] | 后端 API 路由清单 |
| README 中无真实密码 | [ ] | 无 `Admin@123456` 等明文密码 |
| README 中无真实 API Key | [ ] | 无 `sk-` 开头的密钥 |
| README 中无真实 JWT Secret | [ ] | 无真实密钥值 |
| README 中无真实数据库密码 | [ ] | 无真实数据库密码 |
| 环境限制已如实说明 | [ ] | 无 Node / 无 MySQL 限制已说明 |

---

## 五、业务功能检查

| 检查项 | 状态 | 备注 |
|--------|------|------|
| 用户登录流程可用 | [ ] | 需 MySQL 环境验证 |
| 项目创建/查看/更新可用 | [ ] | 需 MySQL 环境验证 |
| 任务创建/查看/更新可用 | [ ] | 需 MySQL 环境验证 |
| AI 生成（Mock）可调用 | [ ] | 不依赖 MySQL |
| 输出编辑/另存版本可用 | [ ] | 需 MySQL 环境验证 |
| 批注添加/查看可用 | [ ] | 需 MySQL 环境验证 |
| 提交审核可用 | [ ] | 需 MySQL 环境验证 |
| 完成审核（评分）可用 | [ ] | 需 MySQL 环境验证 |
| 采用成果可用 | [ ] | 需 MySQL 环境验证 |
| 成果库列表/详情可用 | [ ] | 需 MySQL 环境验证 |
| 统计看板数据展示正常 | [ ] | 部分依赖 MySQL |
| 模型管理页面正常加载 | [ ] | 不依赖 MySQL |
| 路由守卫正确（未登录跳转登录）| [ ] | 前端路由守卫已配置 |

---

## 六、API 接口检查

| 检查项 | 状态 | 备注 |
|--------|------|------|
| 所有接口使用统一返回格式 `{code, message, data}` | [ ] | code=0 表示成功 |
| 无 `/api/auth/register` 接口 | [ ] | 系统中未实现注册接口 |
| 创建任务不使用 `task_name` / `task_type` 字段 | [ ] | 使用 `title` 和 `task_type_id` |
| 登录接口路径为 `/api/auth/login` | [ ] | 已确认 |
| 审核接口使用 `request_id` 而非 `review_id` | [ ] | 已确认 |
| 成果接口使用 `adopted_id` | [ ] | 已确认 |
| 所有路由权限标记正确 | [ ] | 公开/登录用户/管理员/Owner |

---

## 七、安全检查

| 检查项 | 状态 | 备注 |
|--------|------|------|
| `.env` 文件未被提交到 Git | [ ] | `.gitignore` 包含 `.env` |
| `.env.example` 仅含占位符 | [ ] | 无真实密钥 |
| 数据库密码不硬编码 | [ ] | 通过 `os.getenv` 读取 |
| JWT Secret 不硬编码 | [ ] | 通过环境变量读取 |
| API Key 加密存储 | [ ] | AES-GCM 加密 |
| 用户密码哈希存储 | [ ] | BCrypt |
| CORS 配置合理 | [ ] | `allow_origins=["*"]` 适用于开发 |
| 无 SQL 注入风险 | [ ] | 使用参数化查询 |

---

## 八、测试材料检查

| 检查项 | 状态 | 备注 |
|--------|------|------|
| `backend/scripts/test_report_material.md` 测试用例完整 | [ ] | 覆盖认证、项目、任务、审核、成果、统计 |
| 测试用例不含 `/api/auth/register` | [ ] | 已修正 |
| 测试用例不含 `task_name` / `task_type` | [ ] | 已修正 |
| 已知环境限制已说明 | [ ] | WSL2 无法访问 Windows MySQL |
| 未虚构测试通过记录 | [ ] | 诚实说明"待补充截图" |

---

## 九、开源合规检查

| 检查项 | 状态 | 备注 |
|--------|------|------|
| V3 Admin Vite MIT License 保留 | [ ] | `frontend/LICENSE` 存在 |
| NOTICE.md 存在且内容正确 | [ ] | 明确说明模板来源和修改 |
| 未删除原作者版权声明 | [ ] | 保留 Copyright (c) 2022-present pany |
| 未将模板包装为原创 | [ ] | 明确标注"基于 V3 Admin Vite 二次开发" |

---

## 十、课程报告材料对应检查

| 截图/材料 | 是否已有 | 待补充位置 |
|-----------|---------|-----------|
| 数据库建表成功截图 | [ ] | 需本地 MySQL 环境 |
| 关键表结构截图 | [ ] | 需本地 MySQL 环境 |
| 初始数据截图 | [ ] | 需本地 MySQL 环境 |
| 后端启动截图 | [ ] | 需本地 Python 环境 |
| 健康检查截图 | [ ] | 部分需 MySQL |
| Swagger UI 截图 | [ ] | 需本地后端 |
| 语法检查截图 | [ ] | 需本地后端 |
| 前端登录页截图 | [ ] | 需本地 Node 环境 |
| 首页截图 | [ ] | 需本地 Node 环境 |
| 所有业务页面截图 | [ ] | 需本地完整环境 |
| Git 提交记录截图 | [ ] | 需 Git 仓库 |

---

## 总结

完成以上全部检查项（除"待本地验证"项外），即可进行课程报告最终提交。

**仍需本地 Windows 环境验证的事项**：
1. MySQL 数据库初始化和联调
2. 前端 `npm run dev` 和 `npm run build`
3. 登录流程端到端验证
4. 所有业务接口端到端验证
5. 完整前端页面截图

**当前环境已确认可完成的事项**：
1. 后端 Python 文件语法检查（49/49 通过）
2. 后端 API 路由清单（71 个接口已注册）
3. 前端路由清单（13 个路由已配置）
4. Mock AI 生成功能（不依赖 MySQL）
5. 模型管理页面（不依赖 MySQL）
6. 所有文档整理完成
