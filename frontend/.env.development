# Development environment variables (must start with VITE_)

# Vite proxy target: requests to /api/* are forwarded to FastAPI backend
# Backend must run on port 8000: python run.py (default)
VITE_BASE_URL=/
VITE_PUBLIC_PATH=/
