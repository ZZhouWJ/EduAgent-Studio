# EduAgent Studio API 可用性报告

> 核对日期：2026-07-17
> 对应分支：`main`
> 对应架构：FastAPI + MySQL + Redis
> 报告性质：当前基线，替代 2026-06 原型阶段的旧统计

## 1. 当前基线

| 验证项 | 结果 |
| --- | --- |
| OpenAPI 路径 | 115 条 |
| OpenAPI 操作 | 144 个 |
| 后端自动化测试 | 271 项通过，0 失败 |
| Python 编译检查 | `app` 与 `tests` 通过 |
| 依赖一致性 | `pip check` 通过 |
| 运行健康 | API、MySQL、Redis 通过 |
| 前端契约 | TypeScript 检查与 production build 通过 |

OpenAPI 操作数是从当前运行实例的 `/openapi.json` 动态统计，不代表 144 个接口都做了独立手工点击。接口可用性结论来自自动化测试、业务端到端验收和运行健康检查的组合证据。

## 2. OpenAPI 操作分布

| 标签 | 操作数 | 标签 | 操作数 |
| --- | ---: | --- | ---: |
| 任务与版本管理 | 18 | 统计看板 | 17 |
| 课程知识库 | 10 | 提示词模板管理 | 10 |
| 项目管理 | 10 | 学生画像 | 9 |
| 学习任务 | 9 | 认证 | 8 |
| 模型管理 | 7 | 智能体工作台 | 6 |
| 用户与权限 | 6 | 学习辅导 | 5 |
| 审核中心 | 5 | 学习资源 | 4 |
| 平台配置 | 4 | 成果库 | 4 |
| 日志 | 2 | AI 调用与日志 | 2 |
| 学习反馈 | 2 | 多模态 | 1 |
| 对象存储 | 1 | 其他/健康 | 4 |

## 3. 重点已验证合约

- 认证：登录限流、JWT 与服务端会话、登出撤销、禁用用户拦截。
- 课程数据域：管理员、教师和学生只能访问角色与课程允许的资源。
- 知识库：上传签名、容量、文件名、重解析事务、BM25/中文 n-gram 与低相关结果过滤。
- 多智能体：五节点流程、SSE、评测、最多三次修订、checkpoint 恢复和证据链落库。
- Tutor：Provider 无 tool calling 时确定性检索/练习路由、题型/题量约束、会话和有用性反馈落库。
- 反馈闭环：资源状态、测验/自评输入、知识点掌握度与个性化进度。
- 安全：生产 CORS、加密密钥、内容安全双检、错误脱敏、文件访问和反向代理边界。

具体用例组、手工端到端步骤和清理规则见 `docs/测试说明书.md`。

## 4. 外部验证边界

下列项目不纳入“已验证真实外部调用”：

- 讯飞星火真实模型回答。
- 讯飞图像理解/生成、TTS 与 ChatDoc 实际服务。
- Python CVE 扫描（当前验证环境未安装 `pip-audit`）。

对应凭证和服务权限配置后，需补充真实调用记录、时延、Token/成本与失败分支验证；在此之前不将 Mock Provider 结果宣称为真实讯飞 API 结果。

## 5. 复现命令

```bash
cd backend
PYTHONPATH=. python -m unittest discover -s tests -v
python -m compileall -q app tests
python -m pip check

curl http://127.0.0.1:8000/api/health
curl http://127.0.0.1:8000/api/health/db
curl http://127.0.0.1:8000/api/health/redis
curl http://127.0.0.1:8000/openapi.json
```
